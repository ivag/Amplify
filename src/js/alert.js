/**
 * Amp Framework - Tooltips, badges, popovers and labels.
 * 
 * Copyright Quipu GmbH (http://www.quipugmbh.com)
**/
;(function(){
  var Control = Amp.controls.control;
  var badge = _.template("<span class='amp badge'></span>");
  var tip = _.template("");
  
  
  // These methods just extend the Core control prototype
  _.extend(Core.prototype, {
    
    showBadge: function(label, color) {
      
    },
    
    hideBadge: function(label, color) {
      
    },
    
    showTip: function(label, color) {
      
    },
    
    hideTip: function(label, color) {
      
    },
    
    setLabel: function(element) {
      
    }
  });
})();
